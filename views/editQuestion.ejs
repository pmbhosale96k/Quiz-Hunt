<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Edit Question</title>
  <link rel="stylesheet" href="/css/editQue.css" />
</head>

<body>
  <div class="edit-container">
    <h2 class="form-title">✏️ Edit Question</h2>

    <form class="edit-form" action="/admin/update/<%= question._id %>" method="POST">
      <div class="form-group">
        <label>Question</label>
        <input name="question" value="<%= question.question %>" required>
      </div>

      <div class="form-row">
        <div class="form-group">
          <label>Option 1</label>
          <input name="option1" value="<%= question.option[0] %>" required>
        </div>
        <div class="form-group">
          <label>Option 2</label>
          <input name="option2" value="<%= question.option[1] %>" required>
        </div>
      </div>

      <div class="form-row">
        <div class="form-group">
          <label>Option 3</label>
          <input name="option3" value="<%= question.option[2] %>" required>
        </div>
        <div class="form-group">
          <label>Option 4</label>
          <input name="option4" value="<%= question.option[3] %>" required>
        </div>
      </div>

      <div class="form-group">
        <label>Correct Answer</label>
        <input name="correctAnswer" value="<%= question.correctAnswer %>" required>
      </div>

      <div class="form-row">
        <div class="form-group">
          <label>Type</label>
          <input name="type" value="<%= question.type %>" required>
        </div>
        <div class="form-group">
          <label>Difficulty</label>
          <input name="difficulty" value="<%= question.difficulty %>" required>
        </div>
      </div>

      <div class="form-group">
        <label>Tags (comma-separated)</label>
        <input name="tags" value="<%= question.tags.join(', ') %>">
      </div>

      <button class="update-btn" type="submit">Update Question</button>
    </form>
  </div>
</body>

</html>
